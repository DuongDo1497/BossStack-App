import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bossstack.app',
  appName: 'bossstack-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
